import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appsflyer.afexample1',
  appName: 'AFCapacitorApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
